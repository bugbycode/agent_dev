<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bugbycode.mapper.table.TableMapper">
	
	<update id="initHostTable">
		<![CDATA[
			CREATE TABLE IF NOT EXISTS `host` (
				`id` INTEGER PRIMARY KEY AUTOINCREMENT,
				`host` VARCHAR(255),
				`forward` TINYINT(4),
				`result` TINYINT(4),
				`datetime` datetime
			)
		]]>
	</update>
	
	<update id="initUserTable">
		<![CDATA[
			CREATE TABLE IF NOT EXISTS `user` (
				`id` INTEGER PRIMARY KEY AUTOINCREMENT,
				`username` VARCHAR(255),
				`password` VARCHAR(255)
			);
		]]>
		
	</update>
	
	<update id="initSettingTable">
		<![CDATA[
			CREATE TABLE IF NOT EXISTS `setting` (
				`id` INTEGER PRIMARY KEY AUTOINCREMENT,
				`agentPort` TINYINT(4),
				`soBacklog` TINYINT(4),
				`serverPort` TINYINT(4),
				`serverAddress` VARCHAR(255),
				`proxyStatus` TINYINT(4),
				`keystorePassword` VARCHAR(255)
			);
		]]>
	</update>
	
</mapper>